<h2> List of products </h2>

<ul>
  <% @products_array.each do |the_product| %>
    <li>
      <%= the_product.name %>
      - $<%= the_product.price %>
    </li>
  <% end %>
</ul>

<%= link_to "Submit a new product", new_product_path %>
